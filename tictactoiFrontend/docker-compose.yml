version: '3.8'
services:
  web:
    build:
      context: ./app/
      dockerfile: Dockerfile
      args:
        MODE: dev
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    volumes:
      - ./app/:/usr/src/app/
